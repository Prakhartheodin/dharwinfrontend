"use client";

import ResetPasswordPage from "@/app/(components)/(authenticationlayout)/authentication/reset-password/page";

// Route alias so links like `/reset-password?token=...` work
// and render the same reset password UI as `/authentication/reset-password`.
export default ResetPasswordPage;

