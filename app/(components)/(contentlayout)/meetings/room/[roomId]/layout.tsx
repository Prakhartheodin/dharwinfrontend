"use client";

// Full-screen layout for meeting room (no header/sidebar/footer)
export default function MeetingRoomLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
